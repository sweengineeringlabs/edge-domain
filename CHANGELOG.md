# Changelog

## Unreleased

### Added
- `OutboundRegistry<H>` — thread-safe egress handle registry mirroring `HandlerRegistry`.
- `api/traits.rs` — SEA interface contract for the domain layer.
- `src/gateway/` — domain public entry point layer.
