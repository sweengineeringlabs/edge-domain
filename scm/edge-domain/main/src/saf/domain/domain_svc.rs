//! Domain factory methods — implementation lives in `core/domain/domain_svc.rs`
//! (inherent impls must live in the same layer as the type's declaration).
/// SAF module anchor — satisfies arch-audit rule 221.
pub const DOMAIN_SVC: () = ();
