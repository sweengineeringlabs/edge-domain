//! SAF factory identity for [`ObserveBootstrap`](crate::api::ObserveBootstrap).

/// Service identity for the `ObserveBootstrap` factory contract.
pub const OBSERVE_BOOTSTRAP_SVC_FACTORY: &str = "observe_bootstrap_factory";
