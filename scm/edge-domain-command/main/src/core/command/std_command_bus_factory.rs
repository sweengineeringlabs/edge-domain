//! `CommandBusFactory` impl for [`StdCommandBusFactory`].

use crate::api::command::traits::CommandBusFactory;
use crate::api::command::types::StdCommandBusFactory;

impl CommandBusFactory for StdCommandBusFactory {}
