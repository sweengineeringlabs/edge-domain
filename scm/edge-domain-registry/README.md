# edge-domain-registry

The `Registry` port contract — an id-keyed resolution registry of shared entries
(the handler / service / task-controller family), with the in-process
`InMemoryRegistry`.

Part of the [swe-edge](https://github.com/sweengineeringlabs/edge-domain) domain layer.
