//! [`PromptMetadataRequest`] — request for [`Prompt::metadata`](crate::api::prompt::traits::Prompt::metadata).

/// Request for this prompt's metadata. Carries no data.
#[derive(Debug, PartialEq)]
pub struct PromptMetadataRequest;
