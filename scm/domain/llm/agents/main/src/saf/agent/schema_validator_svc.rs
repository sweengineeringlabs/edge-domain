//! Service name constant for the `SchemaValidator` trait.

/// Service identifier for SchemaValidator trait discovery.
pub const SCHEMA_VALIDATOR_SVC: &str = "schema_validator";

pub use crate::api::SchemaValidator;
