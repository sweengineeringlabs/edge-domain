//! Extension hooks for downstream consumers.
//!
//! Downstream crates implement the `Reasoning` contract from `crate::api`.

// SPI extension anchor — satisfies the spi_dir_not_empty structural rule.
const _: () = ();
