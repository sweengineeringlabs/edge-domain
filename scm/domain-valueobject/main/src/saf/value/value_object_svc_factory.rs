//! SAF factory identity for [`ValueObject`](crate::api::ValueObject).

/// Service identity for the `ValueObject` factory contract.
pub const VALUE_OBJECT_SVC_FACTORY: &str = "value_object_factory";
