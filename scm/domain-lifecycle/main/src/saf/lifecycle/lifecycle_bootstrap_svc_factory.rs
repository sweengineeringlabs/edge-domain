//! SAF factory identity for [`LifecycleBootstrap`](crate::api::LifecycleBootstrap).

/// Service identity for the `LifecycleBootstrap` factory contract.
pub const LIFECYCLE_BOOTSTRAP_SVC_FACTORY: &str = "lifecycle_bootstrap_factory";
