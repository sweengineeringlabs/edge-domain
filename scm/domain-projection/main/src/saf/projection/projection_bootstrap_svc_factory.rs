//! SAF factory identity for [`ProjectionBootstrap`](crate::api::ProjectionBootstrap).

/// Service identity for the `ProjectionBootstrap` factory contract.
pub const PROJECTION_BOOTSTRAP_SVC_FACTORY: &str = "projection_bootstrap_factory";
