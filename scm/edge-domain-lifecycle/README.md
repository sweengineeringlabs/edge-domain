# edge-domain-lifecycle

The `Lifecycle` port contract — a generic state-machine abstraction with a
pluggable `TransitionPolicy`, and the in-process `ManagedLifecycle`.

Part of the [swe-edge](https://github.com/sweengineeringlabs/edge-domain) domain layer.
