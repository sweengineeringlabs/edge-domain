//! SAF factory identity for [`QueryableRepository`](crate::api::QueryableRepository).

/// Service identity for the `QueryableRepository` factory contract.
pub const QUERYABLE_REPOSITORY_SVC_FACTORY: &str = "queryable_repository_factory";
