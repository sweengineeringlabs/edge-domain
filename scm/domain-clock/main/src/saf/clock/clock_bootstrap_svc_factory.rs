//! SAF factory identity for [`ClockBootstrap`](crate::api::ClockBootstrap).

/// Service identity for the `ClockBootstrap` factory contract.
pub const CLOCK_BOOTSTRAP_SVC_FACTORY: &str = "clock_bootstrap_factory";
