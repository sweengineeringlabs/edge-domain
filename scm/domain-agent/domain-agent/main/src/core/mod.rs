// Core implementations live in plugins, not here.
// This layer is intentionally empty — Agent primitives are contracts;
// concrete implementations (llmboot, state-machine, etc.) live in edge/plugins/*.
