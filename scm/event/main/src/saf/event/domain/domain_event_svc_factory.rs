//! SAF factory identity for [`DomainEvent`](crate::api::DomainEvent).

/// Service identity for the `DomainEvent` factory contract.
pub const DOMAIN_EVENT_SVC_FACTORY: &str = "domain_event_factory";
