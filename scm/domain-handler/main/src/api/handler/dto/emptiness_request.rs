//! [`EmptinessRequest`] — request for [`HandlerRegistry::is_empty`](crate::api::handler::traits::HandlerRegistry::is_empty).

/// Request to check whether no handlers are registered.
pub struct EmptinessRequest;
