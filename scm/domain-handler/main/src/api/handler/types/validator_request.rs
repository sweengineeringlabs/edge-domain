//! [`ValidatorRequest`] — request for [`Validator::validate`](crate::api::handler::traits::Validator::validate).

/// Request to validate a service-backed handler's configuration.
pub struct ValidatorRequest;
