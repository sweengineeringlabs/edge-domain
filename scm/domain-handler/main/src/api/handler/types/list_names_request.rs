//! [`ListNamesRequest`] — request for [`ServiceRegistry::list_names`](crate::api::handler::traits::ServiceRegistry::list_names).

/// Request to return the names of all registered services.
pub struct ListNamesRequest;
