//! [`BootstrapNameRequest`] — request for `bootstrap_name` on
//! [`HandlerBootstrap`](crate::api::handler::traits::HandlerBootstrap) and
//! [`HandlerProvider`](crate::api::handler::traits::HandlerProvider).

/// Request to query a bootstrap/provider implementation's stable identifier.
pub struct BootstrapNameRequest;
