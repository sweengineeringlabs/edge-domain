//! SAF factory identity for [`RegistryBridge`](crate::api::RegistryBridge).

/// Service identity for the `RegistryBridge` factory contract.
pub const REGISTRY_BRIDGE_SVC_FACTORY: &str = "registry_bridge_factory";
