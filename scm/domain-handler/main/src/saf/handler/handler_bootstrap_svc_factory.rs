//! SAF factory identity for [`HandlerBootstrap`](crate::api::HandlerBootstrap).

/// Service identity for the `HandlerBootstrap` factory contract.
pub const HANDLER_BOOTSTRAP_SVC_FACTORY: &str = "handler_bootstrap_factory";
