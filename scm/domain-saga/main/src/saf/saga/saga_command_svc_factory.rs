//! SAF factory identity for [`SagaCommand`](crate::api::SagaCommand).

/// Service identity for the `SagaCommand` factory contract.
pub const SAGA_COMMAND_SVC_FACTORY: &str = "saga_command_factory";
