//! SAF factory identity for [`PolicyBootstrap`](crate::api::PolicyBootstrap).

/// Service identity for the `PolicyBootstrap` factory contract.
pub const POLICY_BOOTSTRAP_SVC_FACTORY: &str = "policy_bootstrap_factory";
