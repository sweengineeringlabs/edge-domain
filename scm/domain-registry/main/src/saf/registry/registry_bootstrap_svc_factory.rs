//! SAF factory identity for [`RegistryBootstrap`](crate::api::RegistryBootstrap).

/// Service identity for the `RegistryBootstrap` factory contract.
pub const REGISTRY_BOOTSTRAP_SVC_FACTORY: &str = "registry_bootstrap_factory";
