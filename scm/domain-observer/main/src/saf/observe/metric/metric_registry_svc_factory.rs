//! SAF factory identity for [`MetricRegistry`](crate::api::MetricRegistry).

/// Service identity for the `MetricRegistry` factory contract.
pub const METRIC_REGISTRY_SVC_FACTORY: &str = "metric_registry_factory";
