//! `InProcessHandlerRegistry` — SEA Rule 121 api/core mirror.
//!
//! `InProcessHandlerRegistry` is the api-layer type for the in-process handler
//! registry. This path-level mirror lets the structural auditor match
//! `core/handler/in_process_handler_registry.rs` to an api counterpart.
pub use crate::api::handler::types::InProcessHandlerRegistry;
