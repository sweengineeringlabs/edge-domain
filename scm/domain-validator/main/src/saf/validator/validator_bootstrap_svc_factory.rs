//! SAF factory identity for [`ValidatorBootstrap`](crate::api::ValidatorBootstrap).

/// Service identity for the `ValidatorBootstrap` factory contract.
pub const VALIDATOR_BOOTSTRAP_SVC_FACTORY: &str = "validator_bootstrap_factory";
