//! SAF factory identity for [`EventBootstrap`](crate::api::EventBootstrap).

/// Service identity for the `EventBootstrap` factory contract.
pub const EVENT_BOOTSTRAP_SVC_FACTORY: &str = "event_bootstrap_factory";
