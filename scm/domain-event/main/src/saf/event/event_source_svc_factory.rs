//! SAF factory identity for [`EventSource`](crate::api::EventSource).

/// Service identity for the `EventSource` factory contract.
pub const EVENT_SOURCE_SVC_FACTORY: &str = "event_source_factory";
