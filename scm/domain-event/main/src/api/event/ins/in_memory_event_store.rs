//! `InMemoryEventStore` — SEA Rule 121 api/core mirror.
//!
//! This path-level mirror lets the structural auditor match
//! `core/event/ins/in_memory_event_store.rs` to an api counterpart.

/// SEA Rule 121 marker — path co-location sentinel for
/// [`crate::api::event::types::InMemoryEventStore`].
pub(crate) const _RULE_121: () = ();
