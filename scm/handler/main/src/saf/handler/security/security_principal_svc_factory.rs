//! SAF factory identity for [`SecurityPrincipal`](crate::api::SecurityPrincipal).

/// Service identity for the `SecurityPrincipal` factory contract.
pub const SECURITY_PRINCIPAL_SVC_FACTORY: &str = "security_principal_factory";
